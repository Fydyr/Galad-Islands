name: Release Manual

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Numéro de version à publier (ex: 1.0.0)'
        required: true
      branch:
        description: 'Branch source de la release (ex: main ou unstable). Si unstable, la release sera marquée pre-release.'
        required: false
        default: 'main'

permissions:
  contents: write
  packages: write

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller
      - name: Build with PyInstaller
        shell: bash
        run: pyinstaller build.spec

      - name: Archive build
        if: runner.os == 'Windows'
        run: |
          copy RELEASE_README.md dist\galad-islands\README.md
          Compress-Archive -Path dist\galad-islands\* -DestinationPath galad-islands-windows.zip
        shell: pwsh

      - name: Archive build (Linux/Mac)  
        if: runner.os != 'Windows'
        run: |
          cp RELEASE_README.md dist/galad-islands/README.md
          cd dist
          zip -r ../galad-islands-${{ runner.os }}.zip galad-islands

      - name: Upload Release Asset
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ github.event.inputs.version }}
          # Mark prerelease when input branch is 'unstable'
          prerelease: ${{ github.event.inputs.branch == 'unstable' }}
          name: "Galad Islands v${{ github.event.inputs.version }}${{ github.event.inputs.branch == 'unstable' && ' (pre-release)' || '' }}"
          body: |
            🎮 **Galad Islands - Version v${{ github.event.inputs.version }}${{ github.event.inputs.branch == 'unstable' && ' (pre-release)' || '' }}** (Release manuelle)
            
            Téléchargez le jeu pour votre plateforme :
            - **Windows** : `galad-islands-windows.zip`
            - **Linux** : `galad-islands-Linux.zip` 
            - **macOS** : `galad-islands-macOS.zip`
            
            Chaque archive contient :
            - `galad-islands` : Le jeu principal
            - `galad-config-tool` : Outil de configuration autonome (Tkinter)
            
            📖 Consultez le fichier `README.md` inclus dans l'archive pour les instructions d'installation.
            
            🔗 [Documentation complète](https://fydyr.github.io/Galad-Islands/)
            🔗 [Changelog](https://github.com/Fydyr/Galad-Islands/blob/main/CHANGELOG.md)
          files: |
            galad-islands-*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}