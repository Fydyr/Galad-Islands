name: Automatic Release

on:
  push:
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - name: Checkout code at tag
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build with PyInstaller
        shell: bash
        run: |
          # Détection du séparateur selon l'OS
          if [ "$RUNNER_OS" = "Windows" ]; then SEP=';'; else SEP=':'; fi
          # Build main game
          pyinstaller --onefile main.py --name galad-islands \
            --add-data "assets${SEP}assets" \
            --add-data "src/models${SEP}models" \
          # Build config tool
          pyinstaller --onefile --windowed tools/galad_config.py --name galad-config-tool \
                      --add-data "assets/locales${SEP}assets/locales" \
                      --add-data "src${SEP}src" \
                      --exclude-module esper \
                      --exclude-module llvmlite \
                      --exclude-module numba \
                      --exclude-module numpy \
                      --exclude-module Pillow \
                      --exclude-module sklearn \
                      --exclude-module joblib
          # Build Marauder AI Cleaner
          pyinstaller --onefile --windowed tools/maraudeur_ai_cleaner.py --name MaraudeurAiCleaner \
                      --add-data "assets/locales${SEP}assets/locales" \
                      --add-data "src${SEP}src" \
                      --exclude-module esper \
                      --exclude-module llvmlite \
                      --exclude-module numba \
                      --exclude-module numpy \
                      --exclude-module Pillow \
                      --exclude-module sklearn \
                      --exclude-module joblib \

      - name: Detect if tag commit is in 'unstable' branch
        shell: bash
        id: check
        run: |
          # fetch remote branches and check if the commit ($GITHUB_SHA) is contained in unstable
          git fetch --no-tags --prune origin '+refs/heads/*:refs/remotes/origin/*'
          if git branch -r --contains $GITHUB_SHA | grep -q "origin/unstable"; then
            echo "is_unstable=true" >> $GITHUB_OUTPUT
          else
            echo "is_unstable=false" >> $GITHUB_OUTPUT
          fi

      - name: Archive build
        if: runner.os == 'Windows'
        run: |
          mkdir -p galad-islands-windows
          copy dist\galad-islands.exe galad-islands-windows\
          copy dist\galad-config-tool.exe galad-islands-windows\
          copy dist\MaraudeurAiCleaner.exe galad-islands-windows\
          copy RELEASE_README.md galad-islands-windows\README.md
          # Les assets et modèles sont inclus dans l'exécutable via --add-data, pas besoin de les copier
          Compress-Archive -Path galad-islands-windows\* -DestinationPath galad-islands-windows.zip
        shell: pwsh

      - name: Archive build (Linux/Mac)
        if: runner.os != 'Windows'
        run: |
          mkdir -p galad-islands-${{ runner.os }}
          cp dist/galad-islands galad-islands-${{ runner.os }}/
          cp dist/galad-config-tool galad-islands-${{ runner.os }}/
          cp dist/MaraudeurAiCleaner galad-islands-${{ runner.os }}/
          cp RELEASE_README.md galad-islands-${{ runner.os }}/README.md
          mkdir -p galad-islands-${{ runner.os }}/models
          # Les assets et modèles sont inclus dans l'exécutable via --add-data, pas besoin de les copier
          # cp src/models/base_ai_unified_final.pkl galad-islands-${{ runner.os }}/models/
          chmod +x galad-islands-${{ runner.os }}/galad-islands
          chmod +x galad-islands-${{ runner.os }}/galad-config-tool
          chmod +x galad-islands-${{ runner.os }}/MaraudeurAiCleaner
          zip -r galad-islands-${{ runner.os }}.zip galad-islands-${{ runner.os }}

      - name: Upload Release Asset
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          # Use prerelease when the tag's commit is present in unstable branch
          prerelease: ${{ steps.check.outputs.is_unstable == 'true' }}
          name: "Galad Islands ${{ github.ref_name }}${{ steps.check.outputs.is_unstable == 'true' && ' (pre-release)' || '' }}"
          body: |
            🎮 **Galad Islands - Version ${{ github.ref_name }}${{ steps.check.outputs.is_unstable == 'true' && ' (pre-release)' || '' }}**
            
            Téléchargez le jeu pour votre plateforme :
            - **Windows** : `galad-islands-windows.zip`
            - **Linux** : `galad-islands-Linux.zip` 
            - **macOS** : `galad-islands-macOS.zip`
            
            Chaque archive contient :
            - `galad-islands` : Le jeu principal
            - `galad-config-tool` : Outil de configuration autonome (Tkinter)
            - `MaraudeurAiCleaner` : Outil de nettoyage des modèles d'IA (Tkinter)
            
            📖 Consultez le fichier `README.md` inclus dans l'archive pour les instructions d'installation.
            
            🔗 [Documentation complète](https://fydyr.github.io/Galad-Islands/)
            🔗 [Changelog](https://github.com/Fydyr/Galad-Islands/blob/main/CHANGELOG.md)
          files: |
            galad-islands-*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}